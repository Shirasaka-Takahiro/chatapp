<div class="container">
  <h1><%= "#{current_user.username}さんのプロフィール" %></h1>
  <% if current_user.image? %>
    <%= image_tag curret_user.image %>
  <% else %>
    <%= image_tag("no-image-icon-23500.jpg") %>
  <% end %>
  <%= link_to "編集", edit_user_registration_path, class: 'btn' %>
  <p>名前・・・<%= current_user.username %></p>
  <p>紹介文・・・<%= current_user.profile %></p>
  # お気に入り
</div>